<% provide(:title, @user.name) %>

<div class="row">
	<div class="row">
		<div class="col-md-4">
				<%= gravatar_for @user %>
				<h2><%= @user.name %></h2>
		</div>	
	</div>

<%= render 'attributes' %>

<span class="col-md-8">	
	<%= render 'likes' %>
</span>	

</div>			